/* ========================================
   3D CARDS HOVER EFFECT
   Premium smooth tilt effect for cards
   ======================================== */

(function () {
    'use strict';

    // Wait for DOM
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init3DCards);
    } else {
        init3DCards();
    }

    function init3DCards() {
        // 3D Cards Effect Disabled to prevent trembling
        console.log('✨ 3D Cards Effect disabled for stability');
    }
})();
